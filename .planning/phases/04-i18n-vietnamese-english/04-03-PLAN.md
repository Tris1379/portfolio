---
phase: 04-i18n-vietnamese-english
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/components/HeroSection.tsx
  - src/components/StorySection.tsx
  - src/components/ArsenalSection.tsx
  - src/components/GallerySection.tsx
  - src/components/ValuesSection.tsx
  - src/components/SocialLinks.tsx
  - src/components/LanguageSwitcher.tsx
  - src/components/Navbar.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "All 6 section components display resolved locale text, not {en, vi} objects"
    - "LanguageSwitcher toggles between VI|EN with working navigation"
    - "Clicking VI navigates to /vi/ equivalent page"
    - "Clicking EN navigates to /en/ equivalent page"
    - "NEXT_LOCALE cookie is set on language switch"
    - "Current locale is visually bold/highlighted in switcher"
    - "HTML lang attribute reflects current locale"
    - "Page metadata (title, description) translates per locale"
    - "No hardcoded English strings remain in any component"
    - "SocialLinks title reads from config, not hardcoded 'Connect'"
    - "npm run build completes with zero errors"
  artifacts:
    - path: "src/components/HeroSection.tsx"
      provides: "Hero with resolved locale config"
      contains: "getConfig"
    - path: "src/components/StorySection.tsx"
      provides: "Story with resolved locale config"
      contains: "getConfig"
    - path: "src/components/ArsenalSection.tsx"
      provides: "Arsenal with resolved locale config"
      contains: "getConfig"
    - path: "src/components/GallerySection.tsx"
      provides: "Gallery with resolved locale config"
      contains: "getConfig"
    - path: "src/components/ValuesSection.tsx"
      provides: "Values with resolved locale config"
      contains: "getConfig"
    - path: "src/components/SocialLinks.tsx"
      provides: "Social links with config-driven title"
      contains: "getConfig"
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "Working VI|EN toggle with locale navigation"
      contains: "useRouter"
    - path: "src/components/Navbar.tsx"
      provides: "Navbar using resolved config for name"
      contains: "getConfig"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Layout with generateMetadata and lang attribute"
      contains: "generateMetadata"
    - path: "src/app/[locale]/page.tsx"
      provides: "Page passing locale to section components"
      contains: "getConfig"
  key_links:
    - from: "src/components/LanguageSwitcher.tsx"
      to: "src/i18n/routing.ts"
      via: "useRouter and Link from next-intl/routing"
      pattern: "from.*next-intl/routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/config/i18n.ts"
      via: "generateMetadata calls getConfig(locale)"
      pattern: "getConfig.*locale"
    - from: "src/app/[locale]/page.tsx"
      to: "src/config/i18n.ts"
      via: "imports getConfig for resolved config"
      pattern: "import.*getConfig"
    - from: "all section components"
      to: "src/config/i18n.ts"
      via: "import getConfig instead of siteConfig"
      pattern: "getConfig"
    - from: "src/app/layout.tsx"
      to: "DELETED"
      via: "removed — replaced by [locale]/layout.tsx"
    - from: "src/app/page.tsx"
      to: "DELETED"
      via: "removed — replaced by [locale]/page.tsx"
---

<objective>
**Plan 03: Wire i18n into all components and pages**

Purpose: Update every component to use resolved locale config via getConfig(locale) instead of raw siteConfig. Wire LanguageSwitcher with real navigation. Add locale-aware metadata. Delete old root routes so only /[locale]/ routes exist.

Output: All components i18n-aware, LanguageSwitcher working, build passes, zero hardcoded strings remain.
</objective>

<execution_context>
@C:/Users/THINKPAD/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/THINKPAD/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-i18n-vietnamese-english/04-CONTEXT.md
@src/config/site.ts
@src/config/i18n.ts
@src/i18n/routing.ts
@src/components/HeroSection.tsx
@src/components/StorySection.tsx
@src/components/ArsenalSection.tsx
@src/components/GallerySection.tsx
@src/components/ValuesSection.tsx
@src/components/SocialLinks.tsx
@src/components/LanguageSwitcher.tsx
@src/components/Navbar.tsx
@src/app/[locale]/layout.tsx
@src/app/[locale]/page.tsx
@src/app/layout.tsx
@src/app/page.tsx

<interfaces>
From src/config/i18n.ts (Plan 02 output):
```typescript
export type Locale = "en" | "vi";
export function getConfig(locale: Locale): ResolvedSiteConfig;
// Returns flat config — all {en, vi} fields resolved to single string
```

From src/i18n/routing.ts (Plan 01 output):
```typescript
export const routing = defineRouting({
  locales: ["en", "vi"],
  defaultLocale: "en",
});
// Use with: useRouter({ routing }), Link from next-intl/routing
```

Current section component pattern (all 6 follow this):
```typescript
import { siteConfig } from "@/config/site";
export function SectionComponent() {
  // renders siteConfig.sections.X.title, siteConfig.sections.X.items, etc.
}
```

After this plan:
```typescript
import { getConfig } from "@/config/i18n";
import type { Locale } from "@/config/i18n";
export function SectionComponent({ locale }: { locale: Locale }) {
  const config = getConfig(locale);
  // renders config.sections.X.title, config.sections.X.items, etc.
}
```

Current LanguageSwitcher (no-op):
```typescript
"use client";
export function LanguageSwitcher() {
  return (
    <button className="glass px-3 py-1 rounded-full text-sm" aria-label="Switch language">
      <span className="text-(--color-text-secondary)">VI</span>
      <span className="mx-1 text-(--color-text-secondary)">|</span>
      <span className="font-semibold text-(--color-text)">EN</span>
    </button>
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update section components to use getConfig</name>
  <files>src/components/HeroSection.tsx, src/components/StorySection.tsx, src/components/ArsenalSection.tsx, src/components/GallerySection.tsx, src/components/ValuesSection.tsx, src/components/SocialLinks.tsx</files>
  <read_first>src/components/HeroSection.tsx, src/components/StorySection.tsx, src/components/ArsenalSection.tsx, src/components/GallerySection.tsx, src/components/ValuesSection.tsx, src/components/SocialLinks.tsx, src/config/i18n.ts</read_first>
  <action>
    Update ALL 6 section components to accept `locale` prop and use `getConfig(locale)` instead of `siteConfig`. The pattern is identical for each:

    1. **HeroSection.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";

       export function HeroSection({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="min-h-screen flex items-center justify-center">
             <div className="max-w-4xl mx-auto text-center">
               <h1 className="font-[family-name:var(--font-display)] text-5xl md:text-7xl mb-4 text-(--color-text)">
                 {config.name}
               </h1>
               <p className="text-xl md:text-2xl text-(--color-text-secondary) mb-2">
                 {config.title}
               </p>
               <p className="text-base md:text-lg text-(--color-text-secondary)">
                 {config.bio}
               </p>
             </div>
           </section>
         );
       }
       ```

    2. **StorySection.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";

       export function StorySection({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="py-16 md:py-24">
             <div className="max-w-4xl mx-auto px-4 md:px-8">
               <h2 className="font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8 text-(--color-text)">
                 {config.sections.story.title}
               </h2>
               <div className="glass p-6 md:p-8">
                 {config.sections.story.paragraphs.map((paragraph, index) => (
                   <p
                     key={index}
                     className={`text-(--color-text-secondary) ${index < config.sections.story.paragraphs.length - 1 ? "mb-4" : ""}`}
                   >
                     {paragraph}
                   </p>
                 ))}
               </div>
             </div>
           </section>
         );
       }
       ```

    3. **ArsenalSection.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";

       export function ArsenalSection({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="py-16 md:py-24">
             <div className="max-w-4xl mx-auto px-4 md:px-8">
               <h2 className="font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8">
                 {config.sections.arsenal.title}
               </h2>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                 {config.sections.arsenal.categories.map((category) => (
                   <div key={category.name} className="glass p-6">
                     <h3 className="font-[family-name:var(--font-display)] text-xl mb-4">
                       {category.name}
                     </h3>
                     <div className="flex flex-wrap gap-2">
                       {category.skills.map((skill) => (
                         <span
                           key={skill}
                           className="px-3 py-1 rounded-full text-sm border border-(--color-primary)/30 text-(--color-primary)"
                         >
                           {skill}
                         </span>
                       ))}
                     </div>
                   </div>
                 ))}
               </div>
             </div>
           </section>
         );
       }
       ```

    4. **GallerySection.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";
       import { ExternalLink } from "lucide-react";

       export function GallerySection({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="py-16 md:py-24">
             <div className="max-w-4xl mx-auto px-4 md:px-8">
               <h2 className="font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8">
                 {config.sections.gallery.title}
               </h2>
               <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                 {config.sections.gallery.projects.map((project) => (
                   <div key={project.name} className="glass p-6">
                     <h3 className="font-[family-name:var(--font-display)] text-xl mb-2">
                       {project.name}
                     </h3>
                     <p className="text-(--color-text-secondary) mb-4">
                       {project.description}
                     </p>
                     {project.link && (
                       <a
                         href={project.link}
                         target="_blank"
                         rel="noopener noreferrer"
                         className="text-(--color-primary) hover:underline inline-flex items-center"
                       >
                         <ExternalLink className="w-4 h-4 inline mr-1" />
                         View Project
                       </a>
                     )}
                   </div>
                 ))}
               </div>
             </div>
           </section>
         );
       }
       ```

    5. **ValuesSection.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";

       export function ValuesSection({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="py-16 md:py-24">
             <div className="max-w-4xl mx-auto px-4 md:px-8">
               <h2 className="font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8">
                 {config.sections.values.title}
               </h2>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                 {config.sections.values.items.map((item) => (
                   <div key={item.title} className="glass p-6">
                     <h3 className="font-[family-name:var(--font-display)] text-xl mb-2">
                       {item.title}
                     </h3>
                     <p className="text-(--color-text-secondary)">
                       {item.description}
                     </p>
                   </div>
                 ))}
               </div>
             </div>
           </section>
         );
       }
       ```

    6. **SocialLinks.tsx:**
       ```typescript
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";
       import { Github, Linkedin, Mail } from "lucide-react";

       const iconMap: Record<string, React.ReactNode> = {
         github: <Github className="w-5 h-5" />,
         linkedin: <Linkedin className="w-5 h-5" />,
         mail: <Mail className="w-5 h-5" />,
       };

       export function SocialLinks({ locale }: { locale: Locale }) {
         const config = getConfig(locale);
         return (
           <section className="py-16 md:py-24">
             <div className="max-w-4xl mx-auto px-4 md:px-8">
               <h2 className="font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8 text-(--color-text)">
                 Connect
               </h2>
               <div className="flex justify-center gap-6">
                 {config.social.map((link) => (
                   <a
                     key={link.platform}
                     href={link.url}
                     target={link.platform === "email" ? undefined : "_blank"}
                     rel={link.platform === "email" ? undefined : "noopener noreferrer"}
                     aria-label={link.label}
                     className="flex items-center gap-2 text-(--color-text-secondary) hover:text-(--color-primary) transition-colors"
                   >
                     {iconMap[link.icon]}
                     <span className="text-sm text-(--color-text-secondary)">
                       {link.label}
                     </span>
                   </a>
                 ))}
               </div>
             </div>
           </section>
         );
       }
       ```

    NOTE: "Connect" title in SocialLinks stays hardcoded — no title field exists in siteConfig.social. This matches the Phase 3 decision.
  </action>
  <verify>npx tsc --noEmit passes; grep -l "siteConfig" src/components/*Section.tsx src/components/SocialLinks.tsx returns nothing (all switched to getConfig)</verify>
  <done>All 6 section components accept locale prop, import getConfig, display resolved locale text — zero remaining siteConfig imports</done>
</task>

<task type="auto">
  <name>Task 2: Wire LanguageSwitcher and update Navbar</name>
  <files>src/components/LanguageSwitcher.tsx, src/components/Navbar.tsx</files>
  <read_first>src/components/LanguageSwitcher.tsx, src/components/Navbar.tsx, src/i18n/routing.ts</read_first>
  <action>
    1. **LanguageSwitcher.tsx** — Replace no-op with working locale toggle:
       ```typescript
       "use client";

       import { useLocale } from "next-intl";
       import { useRouter, usePathname } from "next-intl/routing";

       export function LanguageSwitcher() {
         const locale = useLocale();
         const router = useRouter();
         const pathname = usePathname();

         function switchLocale(newLocale: "en" | "vi") {
           router.replace(pathname, { locale: newLocale });
         }

         return (
           <button
             className="glass px-3 py-1 rounded-full text-sm"
             aria-label="Switch language"
             onClick={() => switchLocale(locale === "en" ? "vi" : "en")}
           >
             <span
               className={locale === "vi"
                 ? "font-semibold text-(--color-text)"
                 : "text-(--color-text-secondary)"}
             >
               VI
             </span>
             <span className="mx-1 text-(--color-text-secondary)">|</span>
             <span
               className={locale === "en"
                 ? "font-semibold text-(--color-text)"
                 : "text-(--color-text-secondary)"}
             >
               EN
             </span>
           </button>
         );
       }
       ```
       Key behaviors:
       - `useLocale()` returns current locale from next-intl context
       - `useRouter()` from `next-intl/routing` provides typed router with locale support
       - `router.replace(pathname, { locale: newLocale })` navigates to same page in other locale
       - Current locale is bold (`font-semibold`), other is muted (`text-(--color-text-secondary)`)
       - next-intl middleware sets NEXT_LOCALE cookie automatically on navigation

    2. **Navbar.tsx** — Import getConfig for name, keep LanguageSwitcher import:
       ```typescript
       "use client";

       import { getConfig } from "@/config/i18n";
       import { useLocale } from "next-intl";
       import { ThemeToggle } from "@/components/ThemeToggle";
       import { LanguageSwitcher } from "@/components/LanguageSwitcher";

       export function Navbar() {
         const locale = useLocale() as "en" | "vi";
         const config = getConfig(locale);

         return (
           <nav className="glass fixed top-0 left-0 right-0 z-(--z-navbar)">
             <div className="flex justify-between items-center max-w-4xl mx-auto px-4 md:px-8 h-14">
               <span className="font-[family-name:var(--font-display)] text-lg text-(--color-text)">
                 {config.name}
               </span>
               <div className="flex gap-3 items-center">
                 <ThemeToggle />
                 <LanguageSwitcher />
               </div>
             </div>
           </nav>
         );
       }
       ```
       NOTE: getConfig() is a pure function (no async, no server calls) — safe to call in "use client" components. It just resolves {en, vi} objects to flat strings.
  </action>
  <verify>npx tsc --noEmit passes; grep "useRouter" src/components/LanguageSwitcher.tsx shows next-intl/routing import</verify>
  <done>LanguageSwitcher navigates between /en/ and /vi/ with locale toggle; Navbar uses resolved config for name display</done>
</task>

<task type="auto">
  <name>Task 3: Update layout and page, delete old routes</name>
  <files>src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/app/layout.tsx, src/app/page.tsx, src/config/i18n.ts</read_first>
  <action>
    1. **src/app/[locale]/layout.tsx** — Add generateMetadata and pass locale:
       ```typescript
       import type { Metadata } from "next";
       import { Inter, Playfair_Display } from "next/font/google";
       import { NextIntlClientProvider } from "next-intl";
       import { getMessages } from "next-intl/server";
       import { Providers } from "@/components/Providers";
       import { Navbar } from "@/components/Navbar";
       import { getConfig } from "@/config/i18n";
       import type { Locale } from "@/config/i18n";
       import "../globals.css";

       const inter = Inter({
         subsets: ["latin"],
         variable: "--font-body",
         display: "swap",
       });

       const playfair = Playfair_Display({
         subsets: ["latin"],
         variable: "--font-display",
         display: "swap",
       });

       export async function generateMetadata({
         params,
       }: {
         params: Promise<{ locale: string }>;
       }): Promise<Metadata> {
         const { locale } = await params;
         const config = getConfig(locale as Locale);
         return {
           title: `${config.name} — Portfolio`,
           description: `${config.title}`,
         };
       }

       export default async function LocaleLayout({
         children,
         params,
       }: Readonly<{
         children: React.ReactNode;
         params: Promise<{ locale: string }>;
       }>) {
         const { locale } = await params;
         const messages = await getMessages();

         return (
           <html
             lang={locale}
             suppressHydrationWarning
             className={`${inter.variable} ${playfair.variable}`}
           >
             <body className="bg-(--color-bg) text-(--color-text) antialiased font-[family-name:var(--font-body)]">
               <NextIntlClientProvider messages={messages}>
                 <Providers>
                   <Navbar />
                   {children}
                 </Providers>
               </NextIntlClientProvider>
             </body>
           </html>
         );
       }
       ```

    2. **src/app/[locale]/page.tsx** — Pass locale to all section components:
       ```typescript
       import { getLocale } from "next-intl/server";
       import { HeroSection } from "@/components/HeroSection";
       import { StorySection } from "@/components/StorySection";
       import { ArsenalSection } from "@/components/ArsenalSection";
       import { GallerySection } from "@/components/GallerySection";
       import { ValuesSection } from "@/components/ValuesSection";
       import { SocialLinks } from "@/components/SocialLinks";
       import type { Locale } from "@/config/i18n";

       export default async function Home() {
         const locale = (await getLocale()) as Locale;
         return (
           <main>
             <HeroSection locale={locale} />
             <StorySection locale={locale} />
             <ArsenalSection locale={locale} />
             <GallerySection locale={locale} />
             <ValuesSection locale={locale} />
             <SocialLinks locale={locale} />
           </main>
         );
       }
       ```

    3. **Delete old root routes:**
       - Delete `src/app/layout.tsx` (replaced by [locale]/layout.tsx)
       - Delete `src/app/page.tsx` (replaced by [locale]/page.tsx)
       Use: `rm src/app/layout.tsx src/app/page.tsx`
  </action>
  <verify>ls src/app/layout.tsx → should not exist; ls src/app/page.tsx → should not exist; npx tsc --noEmit passes</verify>
  <done>Layout has generateMetadata for locale-aware title/description, page passes locale to all sections, old root routes deleted — only /[locale]/ routes remain</done>
</task>

<task type="auto">
  <name>Task 4: Build verification</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run `npm run build` and verify zero errors.

    If build fails, common causes to check:
    - Old layout.tsx / page.tsx still exist (should be deleted in Task 3)
    - Type mismatch: section component props don't match Locale type
    - Missing import: getConfig not imported in a component
    - next-intl config: routing.ts or request.ts misconfigured
  </action>
  <verify>npm run build exits with code 0</verify>
  <done>Production build passes with zero errors</done>
</task>

</tasks>

<verification>
- Build: `npm run build` completes with zero errors
- No hardcoded imports: `grep -l "import { siteConfig }" src/components/*.tsx` returns nothing
- LanguageSwitcher wired: `grep "useRouter" src/components/LanguageSwitcher.tsx` shows next-intl/routing
- Metadata i18n: `grep "generateMetadata" src/app/[locale]/layout.tsx` exists
- Old routes gone: `test ! -f src/app/layout.tsx && test ! -f src/app/page.tsx`
- HTML lang dynamic: `grep "lang={locale}" src/app/[locale]/layout.tsx` exists
</verification>

<success_criteria>
- [ ] All 6 section components import getConfig, not siteConfig
- [ ] All section components accept `locale: Locale` prop
- [ ] LanguageSwitcher toggles VI|EN with router.replace and visual highlighting
- [ ] Navbar imports getConfig for name display
- [ ] [locale]/layout.tsx has generateMetadata using getConfig
- [ ] [locale]/page.tsx passes locale to all section components
- [ ] src/app/layout.tsx and src/app/page.tsx deleted
- [ ] `npm run build` completes with zero errors
- [ ] No remaining `import { siteConfig }` in any component file
</success_criteria>

<output>
After completion, create `.planning/phases/04-i18n-vietnamese-english/04-03-SUMMARY.md`
</output>

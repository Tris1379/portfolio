---
phase: 03-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navbar.tsx
  - src/components/LanguageSwitcher.tsx
  - src/components/HeroSection.tsx
  - src/components/StorySection.tsx
autonomous: true
requirements:
  - LYOUT-01
  - LYOUT-02
  - SECT-01
  - SECT-02
user_setup: []

must_haves:
  truths:
    - "Fixed navbar displays at top of page with glassmorphism styling and theme toggle"
    - "Navbar includes a VI/EN language switcher pill (visual only, not wired)"
    - "Hero section displays name, title, and bio centered on screen"
    - "Story section presents personal narrative inside a glass card"
    - "No hardcoded strings in any component — all text from site.ts"
    - "All sections use max-w-4xl centered container"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Fixed glassmorphism navbar with ThemeToggle and LanguageSwitcher"
      min_lines: 30
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "VI/EN toggle pill (visual only, wired in Phase 4)"
      min_lines: 15
    - path: "src/components/HeroSection.tsx"
      provides: "Full-viewport hero with name, title, bio from site.ts"
      min_lines: 20
    - path: "src/components/StorySection.tsx"
      provides: "Story narrative in a single .glass card"
      min_lines: 20
  key_links:
    - from: "src/components/Navbar.tsx"
      to: "src/components/ThemeToggle.tsx"
      via: "import and render ThemeToggle"
      pattern: "import.*ThemeToggle"
    - from: "src/components/Navbar.tsx"
      to: "src/config/site.ts"
      via: "import siteConfig for site name"
      pattern: "import.*siteConfig"
    - from: "src/components/HeroSection.tsx"
      to: "src/config/site.ts"
      via: "import siteConfig for name, title, bio"
      pattern: "import.*siteConfig"
---

<objective>
Create the fixed glassmorphism navbar and first two content sections (Hero, Story).

Purpose: Establish the page chrome (navbar with theme toggle + language switcher) and the two most prominent above-fold sections. These are the first things any visitor sees.
Output: 4 new component files — Navbar, LanguageSwitcher, HeroSection, StorySection.
</objective>

<execution_context>
@C:/Users/THINKPAD/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/THINKPAD/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@src/config/site.ts
@src/components/ThemeToggle.tsx
@src/app/globals.css
@src/app/layout.tsx
@src/app/page.tsx

<interfaces>
From src/config/site.ts:
```typescript
export interface SiteConfig {
  name: string;            // "Kazuha"
  title: string;           // "Information Security Professional"
  bio: string;             // one-liner bio
  social: SocialLink[];
  sections: {
    story: StorySection;   // { title, paragraphs: string[] }
    arsenal: ArsenalSection;
    gallery: GallerySection;
    values: ValuesSection;
  };
}

export interface StorySection {
  title: string;
  paragraphs: string[];
}
```

From src/components/ThemeToggle.tsx:
```typescript
export function ThemeToggle(): JSX.Element
// "use client", uses useTheme(), mounted guard, Sun/Moon icons, .glass button
```

From src/app/globals.css — available utilities:
- `.glass` — theme-adaptive glassmorphism (dark: white/10, light: frosted white/80)
- `--z-navbar: 50` — z-index for navbar
- `--color-text`, `--color-text-secondary` — reactive text colors
- `--font-display`, `--font-body` — font variables
- `bg-(--color-bg)`, `text-(--color-text)` — Tailwind v4 CSS var syntax
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Navbar component with ThemeToggle and LanguageSwitcher</name>
  <files>src/components/Navbar.tsx, src/components/LanguageSwitcher.tsx</files>
  <read_first>
    - src/components/ThemeToggle.tsx (existing toggle to import)
    - src/config/site.ts (siteConfig.name for logo text)
    - src/app/globals.css (.glass class, z-index tokens)
  </read_first>
  <action>
Create two new files:

**src/components/LanguageSwitcher.tsx:**
- `"use client"` component
- Renders a pill-shaped button with "VI | EN" text (EN is highlighted/active by default)
- Uses `.glass` class for background consistency
- `className="glass px-3 py-1 rounded-full text-sm"`
- onClick is a no-op placeholder (wired in Phase 4)
- aria-label="Switch language"

**src/components/Navbar.tsx:**
- `"use client"` component (because it imports ThemeToggle which is client)
- Fixed positioning: `className="fixed top-0 left-0 right-0 z-(--z-navbar)"`
- Uses `.glass` class for glassmorphism: `className="glass ..."` applied to the outer nav
- Inner layout: flex, justify-between, items-center, max-w-4xl mx-auto, px-8 (desktop) px-4 (mobile), h-14
- Left side: site name from `siteConfig.name` in Playfair Display font — `font-[family-name:var(--font-display)] text-lg`
- Right side: flex row with `<ThemeToggle />` and `<LanguageSwitcher />` separated by a gap (gap-3)
- Export: `export function Navbar()`

Do NOT modify layout.tsx or page.tsx — that's Plan 03.
  </action>
  <verify>
    <automated>
      test -f src/components/Navbar.tsx && test -f src/components/LanguageSwitcher.tsx && echo "FILES_EXIST" || echo "FILES_MISSING"
    </automated>
  </verify>
  <done>
    - Navbar.tsx exists and exports Navbar function
    - LanguageSwitcher.tsx exists and exports LanguageSwitcher function
    - Navbar imports and renders both ThemeToggle and LanguageSwitcher
    - Navbar uses .glass class and fixed positioning with z-(--z-navbar)
    - Site name rendered from siteConfig.name (not hardcoded)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create HeroSection and StorySection components</name>
  <files>src/components/HeroSection.tsx, src/components/StorySection.tsx</files>
  <read_first>
    - src/config/site.ts (siteConfig.name, .title, .bio, .sections.story)
    - src/app/globals.css (.glass class)
    - src/app/page.tsx (current minimal hero — to be replaced later)
  </read_first>
  <action>
Create two new Server Components (NO "use client" directive):

**src/components/HeroSection.tsx:**
- Import `siteConfig` from `@/config/site`
- Section element with `min-h-screen` (full viewport height), flex, items-center, justify-center
- Inner div: `max-w-4xl mx-auto text-center`
- Name: `<h1>` with `font-[family-name:var(--font-display)] text-5xl md:text-7xl mb-4`
  - Content: `{siteConfig.name}`
- Title: `<p>` with `text-xl md:text-2xl text-(--color-text-secondary) mb-2`
  - Content: `{siteConfig.title}`
- Bio: `<p>` with `text-base md:text-lg text-(--color-text-secondary)`
  - Content: `{siteConfig.bio}`
- Export: `export function HeroSection()`

**src/components/StorySection.tsx:**
- Import `siteConfig` from `@/config/site`
- Section element with `py-16 md:py-24` (generous vertical padding per CONTEXT.md)
- Inner div: `max-w-4xl mx-auto px-4 md:px-8`
- Section title: `<h2>` with `font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8`
  - Content: `{siteConfig.sections.story.title}`
- Single `.glass` card wrapping all paragraphs: `<div className="glass p-6 md:p-8">`
- Map over `siteConfig.sections.story.paragraphs` rendering each as `<p>` with `mb-4 last:mb-0 text-(--color-text-secondary)`
- Export: `export function StorySection()`
  </action>
  <verify>
    <automated>
      test -f src/components/HeroSection.tsx && test -f src/components/StorySection.tsx && echo "FILES_EXIST" || echo "FILES_MISSING"
    </automated>
  </verify>
  <done>
    - HeroSection.tsx exists, renders name/title/bio from siteConfig
    - HeroSection uses min-h-screen for full viewport
    - StorySection.tsx exists, renders story paragraphs in a .glass card
    - StorySection maps over siteConfig.sections.story.paragraphs
    - Both components use max-w-4xl centered containers
    - No hardcoded strings — all text from siteConfig
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` — must pass with zero errors
2. Grep check: `grep -r "hardcoded\|Kazuha\|Information Security" src/components/Navbar.tsx src/components/HeroSection.tsx src/components/StorySection.tsx` — should find NO hardcoded content strings (only siteConfig references)
3. File existence: all 4 component files present in src/components/
</verification>

<success_criteria>
- [ ] Navbar.tsx exists with fixed positioning, .glass, ThemeToggle + LanguageSwitcher
- [ ] LanguageSwitcher.tsx exists as VI/EN pill (visual only)
- [ ] HeroSection.tsx exists with full-viewport layout and siteConfig data
- [ ] StorySection.tsx exists with .glass card wrapping story paragraphs
- [ ] All components import from site.ts — zero hardcoded strings
- [ ] npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-01-SUMMARY.md`
</output>

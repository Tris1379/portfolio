---
phase: 03-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ArsenalSection.tsx
  - src/components/GallerySection.tsx
  - src/components/ValuesSection.tsx
autonomous: true
requirements:
  - SECT-03
  - SECT-04
  - SECT-05
user_setup: []

must_haves:
  truths:
    - "Arsenal section displays 3 category cards in a row on desktop, stacked on mobile"
    - "Each arsenal category contains skill tags as pill elements with accent color border/text"
    - "Gallery section shows project cards with name, description, and external link icon"
    - "Gallery project links open in new tab (target=\"_blank\")"
    - "Values section shows 3 glass cards in a row on desktop, stacked on mobile"
    - "All grid sections collapse to single column at md: breakpoint (768px)"
    - "No hardcoded strings in any component — all text from site.ts"
  artifacts:
    - path: "src/components/ArsenalSection.tsx"
      provides: "Skill categories in .glass cards with pill tags"
      min_lines: 30
    - path: "src/components/GallerySection.tsx"
      provides: "Project cards with name, description, external link"
      min_lines: 25
    - path: "src/components/ValuesSection.tsx"
      provides: "Professional values in .glass cards"
      min_lines: 25
  key_links:
    - from: "src/components/ArsenalSection.tsx"
      to: "src/config/site.ts"
      via: "import siteConfig.sections.arsenal"
      pattern: "siteConfig\\.sections\\.arsenal"
    - from: "src/components/GallerySection.tsx"
      to: "src/config/site.ts"
      via: "import siteConfig.sections.gallery"
      pattern: "siteConfig\\.sections\\.gallery"
    - from: "src/components/GallerySection.tsx"
      to: "lucide-react"
      via: "import ExternalLink icon"
      pattern: "import.*ExternalLink"
---

<objective>
Create the three content-section components: Arsenal (skills grid), Gallery (projects), and Values (professional values).

Purpose: These are the core content sections that fill the page below the Hero and Story. Each uses glassmorphism cards with responsive grid layouts.
Output: 3 new Server Component files — ArsenalSection, GallerySection, ValuesSection.
</objective>

<execution_context>
@C:/Users/THINKPAD/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/THINKPAD/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-content-sections/03-CONTEXT.md
@src/config/site.ts
@src/app/globals.css

<interfaces>
From src/config/site.ts:
```typescript
export interface ArsenalSection {
  title: string;
  categories: {
    name: string;
    skills: string[];
  }[];
}

export interface GallerySection {
  title: string;
  projects: {
    name: string;
    description: string;
    link?: string;
  }[];
}

export interface ValuesSection {
  title: string;
  items: {
    title: string;
    description: string;
  }[];
}
```

From src/app/globals.css — available:
- `.glass` — theme-adaptive glassmorphism class
- `--color-primary` (#a8e6cf) — accent color for skill tag borders/text
- `--color-text-secondary` — secondary text color
- `--font-display` — Playfair Display for headings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArsenalSection with skill category grid and pill tags</name>
  <files>src/components/ArsenalSection.tsx</files>
  <read_first>
    - src/config/site.ts (siteConfig.sections.arsenal — categories with name/skills)
    - src/app/globals.css (.glass class, --color-primary)
  </read_first>
  <action>
Create a Server Component (NO "use client"):

**src/components/ArsenalSection.tsx:**
- Import `siteConfig` from `@/config/site`
- Section element with `py-16 md:py-24`
- Inner div: `max-w-4xl mx-auto px-4 md:px-8`
- Section title: `<h2>` with `font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8`
  - Content: `{siteConfig.sections.arsenal.title}`
- Grid container: `grid grid-cols-1 md:grid-cols-3 gap-6`
- For each category in `siteConfig.sections.arsenal.categories`, render:
  - A `.glass` card: `<div className="glass p-6">`
  - Category name: `<h3>` with `font-[family-name:var(--font-display)] text-xl mb-4`
  - Skills list: `<div className="flex flex-wrap gap-2">`
    - Each skill: `<span className="px-3 py-1 rounded-full text-sm border border-(--color-primary)/30 text-(--color-primary)">`
      - Content: `{skill}`
- Export: `export function ArsenalSection()`
  </action>
  <verify>
    <automated>
      test -f src/components/ArsenalSection.tsx && echo "FILE_EXISTS" || echo "FILE_MISSING"
    </automated>
  </verify>
  <done>
    - ArsenalSection.tsx exists and exports ArsenalSection function
    - Renders 3 category cards in a responsive grid (3 cols desktop, 1 col mobile)
    - Each category card is a .glass element containing pill-shaped skill tags
    - Skill tags use --color-primary for border and text color
    - All text imported from siteConfig — no hardcoded strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GallerySection with project cards and external links</name>
  <files>src/components/GallerySection.tsx</files>
  <read_first>
    - src/config/site.ts (siteConfig.sections.gallery — projects with name/description/link)
    - src/app/globals.css (.glass class)
  </read_first>
  <action>
Create a Server Component (NO "use client"):

**src/components/GallerySection.tsx:**
- Import `siteConfig` from `@/config/site`
- Import `ExternalLink` from `lucide-react`
- Section element with `py-16 md:py-24`
- Inner div: `max-w-4xl mx-auto px-4 md:px-8`
- Section title: `<h2>` with `font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8`
  - Content: `{siteConfig.sections.gallery.title}`
- Grid container: `grid grid-cols-1 md:grid-cols-2 gap-6`
- For each project in `siteConfig.sections.gallery.projects`, render:
  - A `.glass` card: `<div className="glass p-6">`
  - Project name: `<h3>` with `font-[family-name:var(--font-display)] text-xl mb-2`
  - Description: `<p>` with `text-(--color-text-secondary) mb-4`
  - If `project.link` exists, render an `<a>` tag:
    - `href={project.link}`, `target="_blank"`, `rel="noopener noreferrer"`
    - Contains `<ExternalLink className="w-4 h-4 inline mr-1" />` + "View Project" text
    - Styled: `text-(--color-primary) hover:underline inline-flex items-center`
- Export: `export function GallerySection()`
  </action>
  <verify>
    <automated>
      test -f src/components/GallerySection.tsx && echo "FILE_EXISTS" || echo "FILE_MISSING"
    </automated>
  </verify>
  <done>
    - GallerySection.tsx exists and exports GallerySection function
    - Renders project cards in a responsive grid (2 cols desktop, 1 col mobile)
    - Each project card is a .glass element with name, description, and optional link
    - Links open in new tab with ExternalLink icon from lucide-react
    - All text imported from siteConfig — no hardcoded strings
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ValuesSection with professional values cards</name>
  <files>src/components/ValuesSection.tsx</files>
  <read_first>
    - src/config/site.ts (siteConfig.sections.values — items with title/description)
    - src/app/globals.css (.glass class)
  </read_first>
  <action>
Create a Server Component (NO "use client"):

**src/components/ValuesSection.tsx:**
- Import `siteConfig` from `@/config/site`
- Section element with `py-16 md:py-24`
- Inner div: `max-w-4xl mx-auto px-4 md:px-8`
- Section title: `<h2>` with `font-[family-name:var(--font-display)] text-3xl md:text-4xl text-center mb-8`
  - Content: `{siteConfig.sections.values.title}`
- Grid container: `grid grid-cols-1 md:grid-cols-3 gap-6`
- For each item in `siteConfig.sections.values.items`, render:
  - A `.glass` card: `<div className="glass p-6">`
  - Value title: `<h3>` with `font-[family-name:var(--font-display)] text-xl mb-2`
  - Value description: `<p>` with `text-(--color-text-secondary)`
- Export: `export function ValuesSection()`
  </action>
  <verify>
    <automated>
      test -f src/components/ValuesSection.tsx && echo "FILE_EXISTS" || echo "FILE_MISSING"
    </automated>
  </verify>
  <done>
    - ValuesSection.tsx exists and exports ValuesSection function
    - Renders 3 value cards in a responsive grid (3 cols desktop, 1 col mobile)
    - Each card is a .glass element with title and description
    - All text imported from siteConfig — no hardcoded strings
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run build` — must pass with zero errors
2. Grep check: `grep -rE "Security|Development|Infrastructure|Precision|Curiosity|Integrity|Project Alpha|Homelab" src/components/ArsenalSection.tsx src/components/GallerySection.tsx src/components/ValuesSection.tsx` — should find NO hardcoded content strings
3. File existence: all 3 component files present in src/components/
</verification>

<success_criteria>
- [ ] ArsenalSection.tsx exists with grid of category cards and pill skill tags
- [ ] GallerySection.tsx exists with project cards and ExternalLink icons
- [ ] ValuesSection.tsx exists with value cards in responsive grid
- [ ] All grids use grid-cols-1 on mobile, multi-col on md:
- [ ] All components use max-w-4xl centered containers
- [ ] No hardcoded strings — all text from siteConfig
- [ ] npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-sections/03-02-SUMMARY.md`
</output>

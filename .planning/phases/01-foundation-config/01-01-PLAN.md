---
phase: 01-foundation-config
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/favicon.ico
  - .gitignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04
user_setup: []

must_haves:
  truths:
    - "npm run build completes with zero errors"
    - "Glassmorphism utility classes (backdrop-blur, transparency, borders) produce visible glass-card effects"
    - "Z-index token system defined in CSS prevents stacking context conflicts between glassmorphism layers"
    - "Dark mode CSS variant configured for class-based toggling via next-themes"
    - "Design tokens (colors, fonts) defined in CSS @theme block and accessible as Tailwind utilities"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15, React 19, Tailwind CSS v4 dependencies"
      contains: "next@15"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "\"strict\": true"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme config, z-index tokens, glassmorphism utilities, @custom-variant dark"
      contains: "@theme"
    - path: "next.config.ts"
      provides: "Next.js configuration"
    - path: "postcss.config.mjs"
      provides: "PostCSS with @tailwindcss/postcss plugin"
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme block CSS custom properties"
      pattern: "@theme"
    - from: "src/app/globals.css"
      to: "next-themes dark mode"
      via: "@custom-variant dark (&:where(.dark, .dark *))"
      pattern: "@custom-variant dark"
    - from: "src/app/globals.css"
      to: "glassmorphism cards"
      via: "@layer components .glass/.glass-dark classes"
      pattern: "@layer components"
    - from: "src/app/globals.css"
      to: "z-index hierarchy"
      via: "CSS custom properties --z-particles through --z-modal"
      pattern: "--z-"
---

<objective>
Initialize the Next.js 15 project scaffold and CSS design system with Tailwind v4 theme configuration, glassmorphism utilities, z-index token system, and dark mode variant.

Purpose: Establish the design foundation that all subsequent phases depend on. Every component in Phases 2-6 imports from globals.css tokens and uses glassmorphism classes.
Output: Working Next.js 15 project with Tailwind v4 configured, root layout, design tokens, z-index system, glassmorphism utilities.
</objective>

<execution_context>
@C:/Users/THINKPAD/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/THINKPAD/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-config/01-CONTEXT.md

<interfaces>
No existing code interfaces — this is a greenfield scaffold plan. All interfaces are being created.

Key decisions from CONTEXT.md (MUST honor):
- Z-index tokens: `--z-particles: 0`, `--z-content: 10`, `--z-cards: 20`, `--z-navbar: 50`, `--z-modal: 100`
- Glass variants: `.glass` (light overlay) and `.glass-dark` (subtle), in `@layer components`
- Font: Playfair Display (display) + Inter (body) via next/font/google
- Colors: primary `#a8e6cf`, surface `#0a0a0a`, surface-light `#1a122e`
- Tailwind v4 uses `@theme` directive in CSS (NO tailwind.config.js)
- Dark mode via `@custom-variant dark (&:where(.dark, .dark *))`

Research findings (pitfalls to avoid):
- Pitfall 13: Tailwind v4 dark mode MUST use @custom-variant, not darkMode: 'class' in config
- Pitfall 14: Safari needs -webkit-backdrop-filter prefix AND @supports fallback
- Pitfall 2: backdrop-filter creates stacking contexts — z-index tokens must be defined before any components
- Pitfall 3: Theme FOUC — suppressHydrationWarning on html tag
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and configure CSS design system</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    .planning/phases/01-foundation-config/01-CONTEXT.md — Locked decisions for z-index values, glass variants, colors, fonts
    .planning/research/STACK.md — Package versions and Tailwind v4 @theme config pattern
    .planning/research/PITFALLS.md §Pitfall 2, 13, 14 — z-index stacking, Tailwind dark variant, Safari prefix
  </read_first>
  <action>
    1. Scaffold Next.js 15 project in current directory (D:\DH\Code\portfolio):
       ```
       npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --turbopack
       ```
       If create-next-app prompts, select: TypeScript yes, ESLint yes, Tailwind yes, src/ directory yes, App Router yes, import alias @/*, Turbopack no (for stable builds).
       If project files already exist from partial setup, continue to step 2.

    2. Ensure Tailwind v4 configuration. In postcss.config.mjs, use:
       ```js
       const config = {
         plugins: {
           "@tailwindcss/postcss": {},
         },
       };
       export default config;
       ```
       Remove any tailwind.config.js/ts if create-next-app generated one (Tailwind v4 uses CSS @theme, not JS config).

    3. Write src/app/globals.css with complete design system:
       ```css
       @import "tailwindcss";

       @theme {
         --color-primary: #a8e6cf;
         --color-surface: #0a0a0a;
         --color-surface-light: #1a122e;

         --font-display: "Playfair Display", serif;
         --font-body: "Inter", sans-serif;
       }

       @custom-variant dark (&:where(.dark, .dark *));

       :root {
         --z-particles: 0;
         --z-content: 10;
         --z-cards: 20;
         --z-navbar: 50;
         --z-modal: 100;
       }

       @layer components {
         .glass {
           background: rgba(255, 255, 255, 0.1);
           backdrop-filter: blur(16px);
           -webkit-backdrop-filter: blur(16px);
           border: 1px solid rgba(255, 255, 255, 0.2);
           border-radius: 0.75rem;
         }

         .glass-dark {
           background: rgba(0, 0, 0, 0.2);
           backdrop-filter: blur(16px);
           -webkit-backdrop-filter: blur(16px);
           border: 1px solid rgba(255, 255, 255, 0.1);
           border-radius: 0.75rem;
         }
       }

       @supports not (backdrop-filter: blur(12px)) {
         .glass {
           background: rgba(0, 0, 0, 0.8);
         }
         .glass-dark {
           background: rgba(0, 0, 0, 0.9);
         }
       }
       ```

    4. Overwrite src/app/layout.tsx with placeholder (Task 2 will add fonts and Providers):
       ```tsx
       import type { Metadata } from "next";
       import "./globals.css";

       export const metadata: Metadata = {
         title: "Kazuha — Portfolio",
         description: "Information Security Professional Portfolio",
       };

       export default function RootLayout({
         children,
       }: Readonly<{
         children: React.ReactNode;
       }>) {
         return (
           <html lang="en" suppressHydrationWarning>
             <body className="bg-[#0a0a0a] text-white antialiased">
               {children}
             </body>
           </html>
         );
       }
       ```

    5. Verify build passes:
       ```
       npm run build
       ```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - `npm run build` completes with zero errors
    - src/app/globals.css contains @theme block with --color-primary: #a8e6cf, --color-surface: #0a0a0a
    - src/app/globals.css contains @custom-variant dark (&:where(.dark, .dark *))
    - src/app/globals.css contains :root block with --z-particles: 0, --z-content: 10, --z-cards: 20, --z-navbar: 50, --z-modal: 100
    - src/app/globals.css contains @layer components with .glass class including backdrop-filter and -webkit-backdrop-filter
    - src/app/globals.css contains @supports fallback for backdrop-filter
    - postcss.config.mjs references @tailwindcss/postcss (not postcss-import or autoprefixer)
    - No tailwind.config.js/ts file exists (Tailwind v4 uses CSS @theme)
  </done>
</task>

</tasks>

<verification>
- Verify globals.css has all required design tokens via grep:
  - `@theme` block present
  - `@custom-variant dark` present
  - `--z-particles`, `--z-content`, `--z-cards`, `--z-navbar`, `--z-modal` present
  - `.glass` and `.glass-dark` classes present
  - `-webkit-backdrop-filter` present (Safari compatibility)
  - `@supports` fallback present
- Verify postcss.config.mjs has @tailwindcss/postcss (not old Tailwind v3 plugins)
- Verify no tailwind.config.js/ts exists
- Verify build passes with zero errors
</verification>

<success_criteria>
- Next.js 15 project scaffolded with TypeScript, App Router, src directory
- Tailwind CSS v4 configured via @theme in globals.css (no tailwind.config.js)
- Dark mode variant configured for class-based toggling
- Z-index token system with named custom properties (5 tokens with gaps)
- Glassmorphism utility classes (.glass, .glass-dark) in @layer components
- Safari compatibility (-webkit-backdrop-filter prefix + @supports fallback)
- Design tokens (primary, surface, surface-light colors) accessible as Tailwind utilities
- `npm run build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config/01-01-SUMMARY.md`
</output>
